import React from "react";
import { View, Text, StyleSheet } from "react-native";

const Comando = (props) => {
    return (
        <View>
            <Text>Comando</Text>
        </View>
    );
};

export default Comando;